" this is comment for this sample code which
converts Fahrenheit into if Celcius "

function convert (fahr integer)
{
    return 5 * (fahr - 32) / 9;
    5 == 5;
    5 <= 6;
    123.123.123.123
}

if
IF

#
integer low, high, step;

get (low, high, step);
while (low <= high)  
{
    put (low);
    put (convert (low));
    low = low + step;
} 
#